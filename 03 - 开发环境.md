## jre

sudo pacman -S jre17-openjdk

## npm

`sudo pacman -S npm`

配置 npm 镜像

`npm config set registry https://registry.npmmirror.com`

## python

1.安装tk

`sudo pacman -S tk`

2.pip镜像

`pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple`

3.虚拟环境

```bash
python -m venv .venvs/learn
source /home/0x2a/.venvs/learn/bin/activate # 将此行写入 .zshrc
```

4.jupyterlab

```bash
pip install jupyterlab jupyterlab-widgets
jupyter lab --generate-config
pip install jupyterlab-language-pack-zh-CN
```

5.注册内核

```bash
python -m ipykernel install --user --name learn --display-name LearnAI

--name learn # 虚拟环境名称
--display-name LearnAI # 内核名称
```

6.模块

```bash
###############################################################################
# 标准库
###############################################################################
Base64
csv
json
sqlite3
webbrowser   操纵当前系统的默认浏览器，访问指定 URL 的页面

psutil 是一个跨平台的进程管理和系统工具库，可以处理”CPU、内存、磁盘、网络、用户“等信息。 主要用于系统资源的监控，分析，以及对进程进行一定的管理。
platform  这个模块提供了很多用于获取操作系统的信息的功能。
sys  这个模块可供访问由解释器使用或维护的变量和与解释器进行交互的函数。
os 提供了常见的操作系统相关功能，很多功能是针对文件系统。
shutil 提供了一些比较高级的文件和目录操作（目录递归复制、目录递归删除、目录压缩打包...）

glob 用于查找文件
math 常用的数学函数
random  用来生成随机数
re 基于正则的匹配和替换
###############################################################################
# 第三方库
###############################################################################

# 爬虫
pip install httpx
pip install bs4
pip install lxml
pip install selenium
pip install Scrapyl

# 数据分析
pip install numpy
pip install scipy
pip install pandas
pip install matplotlib

# 数学
pip install sympy
pip install statsmodels

# ML
pip install scikit-learn
pip install nltk

# 其他
pip install sh
pip install astropy
pip instll sunpy
pip install Pillow 
pip install pygame
pip install graphviz
```
